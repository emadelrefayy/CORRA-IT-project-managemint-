import { NavLink, useLocation } from 'react-router-dom';
import { LayoutDashboard, Boxes, Network, Wrench, FolderKanban, Menu } from 'lucide-react';
import type { ReactNode } from 'react';
const nav=[['/','Overview',LayoutDashboard],['/projects','Projects',FolderKanban],['/assets','Assets',Boxes],['/network','Network',Network],['/maintenance','Maintenance',Wrench]] as const;
export function Layout({children}:{children:ReactNode}){const [location]=useLocation(); const section=nav.find(n=>n[0]===location)?.[1]||'Command center'; return <div className="shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">KI</div><div><strong>CORRA IT</strong><small>KORRA COMMAND CENTER</small></div></div><div className="nav-label">Operations</div><nav className="nav">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>isActive?'active':''}><Icon size={15} strokeWidth={1.7}/><span>{label}</span></NavLink>)}</nav><div className="side-note"><b>Field-ready workspace</b>Live operational records for construction-site infrastructure. Data is read-only in this MVP.</div></aside><main className="main"><header className="topbar"><button className="mobile-menu" aria-label="Open navigation"><Menu size={20}/></button><div className="crumb">CORRA / {section}</div><div className="top-actions"><span className="read-only">Read only</span><span className="avatar">IT</span></div></header>{children}</main></div>}